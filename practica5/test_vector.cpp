#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<vector<int>> t(10, vector<int>(3)); // el primer valor son las filas, columnas

    for(int i=0; i<t.size(); i++){ // columnas
        for(int j=0; j<t[i].size(); j++){ // filas 
            cout<<"hola ";
        }
        cout<<endl;
    }
    return 0;
}